{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "command": "cmake \"CMakeLists.txt\" ${workspaceFolder} && cmake --build .",
            "detail": "Build using CMake"
        }
    ],
    "version": "2.0.0"
}